PORT=4000
JWT_SECRET=dev_secret
TOKEN_EXPIRES_IN=7d
DATABASE_URL=postgres://postgres:password@localhost:5432/wmsu_hr_connect
CORS_ORIGIN=http://localhost:5173
TRUST_PROXY=false

# Performance notes:
# - DB pool: max 20 connections for concurrent request handling
# - Auth rate limit: 200 requests per 15 min (prevents brute force while allowing bulk users)
# - Audit logging: fire-and-forget (async, non-blocking)
# - DB indexes: created on audit_logs table for fast queries
